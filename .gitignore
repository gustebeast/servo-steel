# Onshape credentials (real keys) — never commit
tools/onshape_credentials.json
tools/build_counter.txt

# Python
__pycache__/
*.pyc
.venv/

# Build artifacts (regenerated by `py -3.12 -m src.build`)
*.step
*.stl
*.png
